Solve:

1. вызов function generateBoard(строка) 
return двухмерный массив (цифры или строки)

2. Запуск цикл который перебирает каждое значение и проверяет на условия логики
  2.1. проверка является ли ячейка заполненной
  2.2. если не заполнена вызываем три функции
    2.2.1. вызов функции создание массива строки function createRow(доска) => массив строки
    2.2.2. вызов функции создание массива столбца function createColumn(доска) => массив столбца

  2.3. Объединяем массивы и создаем массив вариантов затем вычеркиваем все элементы из массива вариантов
  2.4. Если остался один элемент то заполняем им ячейку и прибавляем счетчику 1;
  2.5. Если цикл прошел и изменений нет выходим из цикла. 

return решение в виде двухмерного массива (цифры или строки) 


БУДУЩЕЕ

    2.2.3. вызов функции создание массива квадрата function createSquare(доска) => массив квадрата
