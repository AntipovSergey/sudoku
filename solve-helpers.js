const getRow = (n) => {
  
  // returns array
}

const getColumn = (n) => {
  
  // returns array
}

const getSquare = (n) => {
 
  // return array
}

//
// Функция, проверяющая массив на валидность
//


module.exports = {
  getRow,
  getColumn,
  getSquare
}
