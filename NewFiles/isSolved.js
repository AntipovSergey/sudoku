/**
 * Принимает игровое поле в том формате, в котором его вернули из функции solve.
 * Возвращает булевое значение — решено это игровое поле или нет.
 */
function isSolvedFromNEwFile(board) {
  return ('Hello');
}

console.log(isSolvedFromNEwFile());

module.exports = {
  isSolvedFromNEwFile,
};
