//Функция которая преобразует строку данных в вид массива масиивов (матрицы)
solve (boardString)
return board 
Катя

// Функция которая принимает судоку в виде таблицы и номер строки в которой
//находится пустая ячейка и n - числа от 1 до 9 возвращает true если n можно 
//подставить
validRow (board, row_index, n)
return true/false
Александр Л

// Функция которая принимает судоку в виде таблицы и номер столбца в которой
//находится пустая ячейка и n - числа от 1 до 9 возвращает true если n можно 
//подставить
validCol (board, col_index, n)
return true/false
Александр Ч

// Функция которая принимает судоку в виде таблицы и номер строки и столбца в которой
//находится пустая ячейка и n - числа от 1 до 9 возвращает true если n можно 
//подставить
validBox (board, row_index, col_index, n)
return true/false
Ольга 
